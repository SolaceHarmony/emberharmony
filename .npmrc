# EmberHarmony requires Bun as the package manager
# If you see this, you should be using: bun install

# Enforce engine and package manager requirements
engine-strict=true

# Disable package script approvals (not recommended, use Bun instead)
ignore-scripts=false
