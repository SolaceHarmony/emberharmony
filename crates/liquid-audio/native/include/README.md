# Native ABI Headers

Place shared C/C++ ABI headers here when an engine interface needs to be consumed
by more than one translation unit or mirrored from Rust.

Current native entry points are still declared privately in the Rust FFI rim, so this
directory is intentionally small.
